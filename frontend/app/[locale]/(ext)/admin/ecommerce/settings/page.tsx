import SettingsConfiguration from "./client";

export const metadata = {
  title: "Settings | Admin",
  description: "Manage ecommerce settings in the admin dashboard",
};

export default function Page() {
  return <SettingsConfiguration />;
}
