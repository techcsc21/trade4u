"use client";
import LanguageSelector from "./language-selector";

// Keep the existing component for backward compatibility
const Language = () => {
  return <LanguageSelector variant="default" />;
};

export default Language;
