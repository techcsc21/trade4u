# Suppress npm warnings for unknown config options
# These are set globally but not needed for this project
loglevel=error

# Ensure we use the default registry
registry=https://registry.npmjs.org/

# Disable fund messages
fund=false

# Set audit level to moderate
audit-level=moderate